---
date: 2026-06-04
repo: PDFHorse
status: open
resume: "verder met pdfhorse — sign-feature (3 modi bitmap/SVG/live) na v0.3.0-Putman fill"
---

# Sessie 2026-06-04 — v0.3.0-Putman: fill + P2-docs

**Agent:** Claude Opus 4.7 (1M context)
**Repo:** PDFHorse (cpaglebbeek/PDFHorse)
**Branche:** main
**Cross-repo werk:** Meta_Master (claude_memory + STATUS update bij OEU)
**Eindstand commits:** zie `git log` (hash deze sessie nog niet bekend op moment van schrijven)

---

## Opdracht (samengevat)

Gebruiker zei "negeer limieten, bouw verder". Ik koos pragmatisch: P2-docs uit /sanitycheck-backlog + derde werkende feature (fill). Sign blijft voor v0.4.0 vanwege complexiteit (3 modi + fabric.js + signature_pad).

---

## Prompts en acties — chronologisch

### Prompt 1 — "negeer limieten, bouw verder"
**Actie:**
- `docs/PRINCIPLES.md` NIEUW (8 principes P1-P8: Client-first / CDN-only / Stateless server / Geen account+tracking / Mail-only no archief / AGPL copyleft / Limieten als ontwerp / Docs is feature). Elk principe: *waarom* + *toepassing* + *conflict-test*
- `docs/PRIVACY.md` NIEUW (volledige verklaring: client-side vs server-side scope, OCR-flow, mail-flow, bewaartermijnen, doorgifte (Tesseract lokaal HC55 + Hostinger SMTP), beveiliging, AVG-rechten, AGPL-audit)
- `docs/BUGLIST.md` NIEUW (skelet uit Meta_Master/templates/BUGLIST_TEMPLATE.md, 0 bugs, format-blok voor toekomstige entries inclusief verplichte 3-niveau RCA)
- `CHANGELOG.md` NIEUW (5 entries v0.0.1 → v0.3.0, Keep-a-Changelog-stijl met PDFHorse-eigen codenamen)
- `version.json` → 0.3.0 / Putman / third-feature
- `frontend/index.html`: header v0.3.0-Putman + PDF.js 4.0.379 module-script + worker-config + fill-section volledig herschreven (drop-zone, file-info-bar met fontSize-input, gerenderde pages in <canvas>, klik-overlay genereert tekstvelden met live-edit + ✕ verwijder, Download-knop + Wissen)
- `frontend/js/app.js`: `fill`-state + `_fillLoad()` (PDF-valid + pdf-lib load + render via PDF.js per page in `<canvas>`) + `_waitForPdfJs()` (polling tot async module-load klaar) + `addFillField()` (canvas-coords) + `removeFillField()` + `fillReset()` + `runFill()` (canvas→PDF coords-transform met scaleX/scaleY + pdf-lib drawText + Helvetica embed + download)
- `ARCHITECTURE.md` fill-flow geactualiseerd ("geïmplementeerd in v0.3.0-Putman" met coords-transform formule)
- `docs/DEPENDENCIES.md`: PDF.js 4.0.379 toegevoegd + versiehistorie-rij v0.3.0
- `ACTIONS.md`: fill afgevinkt + 4 P2-docs afgevinkt, sectie hernoemd v0.3.0 → v0.4.0
- Tests: pytest 4/4 groen (backend ongewijzigd), `node --check` OK, Node smoke met 2-page A4 PDF + 3 velden (2 op page 0, 1 op page 1, verschillende fontSize) → output 1664 bytes vs src 1050 bytes (groter = drawText werkt) ✓
**Keuze:** Geen sign deze ronde (3 modi vereist meer libs); sign wordt v0.4.0. Geen output-bar (print/mail) activeren — wacht op Hostinger mailbox. Fontsize-input per veld via globale default + onthouden van laatst-gekozen waarde.

---

## Belangrijke keuzes deze sessie

| Keuze | Reden |
|---|---|
| Fill vóór sign | Eenvoudiger (geen 3 modi); blocking eerste echte "edit"-feature |
| PDF.js voor preview i.p.v. raw canvas-clicks zonder beeld | Zonder visuele referentie kan gebruiker niet plaatsen; PDF.js is de browser-standaard |
| PDF.js via cdnjs i.p.v. unpkg | unpkg leverde geen .mjs-module schoon; cdnjs heeft pdf.min.mjs + pdf.worker.min.mjs direct |
| Module-script + dynamic import + polling-fallback | PDF.js 4.x is ESM-only en laadt async; polling op `window.pdfjsLib` is robuust |
| Canvas-coords (top-left) → PDF-coords (bottom-left) | Standaard transform: `pdfY = pdfHeight - (canvasY * scaleY)`; per page apart geschaald |
| Per-veld fontSize + globale default | UX: meestal 1 grootte voor hele formulier; per-veld override mogelijk via state |
| Sign uitstellen tot v0.4.0 | 3 modi (bitmap/SVG/live) + 2 nieuwe libs (fabric.js + signature_pad) = aparte oranje bump waard |
| 4 P2-docs in één klap | Backlog leeggewerkt vóór feature-werk = nul-debt benadering |
| Geen WhatIf-pauzes deze ronde | Gebruiker zei "negeer limieten, bouw verder" — directieve override |

---

## Open eindjes na deze sessie

**Klaar:**
- v0.3.0-Putman runnable lokaal — open `frontend/index.html` direct in browser
- Backend ongewijzigd; geen HC55-redeploy nodig
- P2-doc-backlog uit /sanitycheck volledig weggewerkt (PRINCIPLES + PRIVACY + BUGLIST + CHANGELOG)

**Open / volgende sessie:**
- **v0.4.0 Sign-feature** (3 modi: bitmap upload + SVG upload + signature_pad live-canvas) — vereist 2 nieuwe CDN-libs
- **v0.5.0 Output-bar** (Download + Print + Mail-knoppen activeren voor laatste output)
- **OCR + mail backend** — wacht op Hostinger mailbox-creds
- **HC55 deploy** van merge+split+fill-versie — kan al
- **Cross-repo nginx** `/PDFHorse/` in iCt_Horse repo + footer-link
- **docs/screens/** — screenshots per tab na browser-test
- **SRI-hashes** op alle CDN-scripts (security-hardening)
- **Browser-test door gebruiker:** fill-tab → upload PDF → page-render? → klikken → tekst typen → fontSize wijzigen → Download → openen filled PDF visueel verifiëren

**Multi-session-incidenten in voorafgaande rondes:**
3× scoop in dezelfde sessie (HeeresReformed `f221015`, Anika `70654a2+e83502a`, PortalToExact `e18f4c0`); nieuwe `feedback_oeu_selectieve_git_add.md` aangelegd in eerdere sub-OEU.

---
date: 2026-06-04
repo: PDFHorse
status: done
resume: ""
---

# Sessie 2026-06-04 — HC55 MVP-deploy v0.4.0-Taft (frontend + backend live)

**Agent:** Claude Opus 4.7 (1M context)
**Repo:** PDFHorse + nginx-config HC55
**LIVE URL:** `https://horsecloud55.ddns.net/PDFHorse/`

## Opdracht

Optie B uit deploy-keuze: backend + frontend op HC55, MVP zonder OCR (Tesseract niet geïnstalleerd) en zonder mail (Hostinger mailbox bestaat nog niet).

## Uitgevoerd

1. Pre-flight checks via ssh: Python 3.12.3 + nginx 1.24 aanwezig, poort 3963 vrij, paden bestaan nog niet, Tesseract niet geïnstalleerd
2. Backup: `/etc/nginx/sites-enabled/horsecloud.bak.20260604_232651_pre_pdfhorse` (15010 bytes)
3. System deps: `apt install python3-venv python3-pip git` (idempotent)
4. User + dirs: `useradd -r pdfhorse` + `/opt/pdfhorse` + `/var/www/pdfhorse` + `/tmp/pdfhorse` + `/opt/pdfhorse/logs`
5. Code: `git clone github.com/cpaglebbeek/PDFHorse → /opt/pdfhorse`
6. Python venv + `pip install fastapi==0.115.0 uvicorn==0.32.0 python-multipart slowapi pytest httpx`
7. Dummy `.env` (mode 600, alleen `PDFHORSE_ENV=production`)
8. Frontend rsync naar `/var/www/pdfhorse/frontend/`
9. Systemd unit geïnstalleerd, enable+start → active+running, 82MB memory, 2 workers
10. Lokale smoke `curl http://127.0.0.1:3963/api/health` → 200 OK
11. Nginx-snippet + `include` in horsecloud server-block 443-ssl via awk-insert
12. `nginx -t` → syntax OK (pre-existing .bak-warnings genegeerd) → reload
13. **Eerste publieke curl: 401** — server-level `auth_basic "HorseCloud"` erft naar /PDFHorse/
14. Fix: snippet uitgebreid met `auth_basic off;` per location (patroon van snippets/horsesafe.conf), scp + reload
15. Publieke smoke geslaagd: health + limits JSON + frontend HTML
16. Regressie-check: HorseSafe 200, Dashboard 302, Archi 301 — geen schade
17. Backend version-drift fix: `backend/main.py` las hardcoded `0.0.2/Warnock`; nu dynamisch uit `version.json` (single source of truth). `ALLOWED_ORIGIN` default naar `horsecloud55.ddns.net`. Test aangepast.
18. HC55 redeploy: git pull + `systemctl restart pdfhorse` → backend toont juiste versie 0.4.0/Taft

## Belangrijke keuzes

| Keuze | Reden |
|---|---|
| MVP-deploy zonder Tesseract | OCR is sowieso geblokkeerd op mailbox; geen waarde door 500MB-install nu |
| URL op horsecloud55.ddns.net i.p.v. icthorse.nl | Hostinger reverse-proxy is aparte stap; patroon-conform HorseSafe |
| Backup + nginx -t vóór reload | Shared-infra P-TECH-04, voorkomt downtime 40+ andere endpoints |
| `auth_basic off;` per location | Server-level auth_basic erft anders; PDFHorse moet publiek/anoniem zijn |
| Version dynamisch uit version.json | Voorkomt herhalen drift-bug bij elke release |
| Lege .env voor systemd EnvironmentFile | Anders faalt service-start; placeholder tot SMTP-creds beschikbaar |

## Open

- OCR backend → Tesseract + ocrmypdf install + endpoint implementatie
- Mail backend → wacht op Hostinger mailbox pdfservice@icthorse.nl
- icthorse.nl/PDFHorse/ → Hostinger reverse-proxy of mirror
- docs/screens/ → screenshots nu mogelijk vanaf live URL
- SRI-hashes op CDN-scripts
- HC55 nginx warnings: pre-existing `.bak`-files veroorzaken `conflicting server name` warnings; cleanup-actie open (geen functie-impact)

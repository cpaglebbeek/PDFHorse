---
date: 2026-06-05
repo: PDFHorse
status: open
resume: "verder met pdfhorse — mail backend (Hostinger mailbox aanmaken) + icthorse.nl reverse-proxy na v0.8.0-Reid OCR"
---

# Sessie 2026-06-05 — v0.8.0-Reid: OCR backend

**Agent:** Claude Opus 4.7 (1M, op ~91% context bij start — directieve override "bouw")
**Repo:** PDFHorse + HC55 redeploy + apt tesseract
**LIVE URL:** `https://horsecloud55.ddns.net/PDFHorse/`

## Uitgevoerd

- `version.json` → 0.8.0 / Reid
- `backend/requirements.txt`: `ocrmypdf==16.5.0` actief
- `backend/main.py`: `MAX_OCR_BYTES=50MB`, `OCRMYPDF_BIN`, `OCR_LANGUAGES=nld+eng`, `OCR_TIMEOUT_S=180s`; `POST /api/ocr` endpoint (was 501-stub) → ocrmypdf --skip-text --output-type pdf --quiet → FileResponse + BackgroundTask cleanup; HTTP 415/400/413/502/503/504
- `backend/tests/test_health.py`: 3 nieuwe tests (415 non-pdf, 400 leeg, 503 zonder ocrmypdf via monkeypatch)
- `frontend/index.html`: header v0.8.0 + OCR-tab functioneel (drop-zone + Verwerken-knop + busy-state)
- `frontend/js/app.js`: `ocr`-state + `runOcr` (POST /api/ocr → arrayBuffer → download + `_setOutput`)
- ACTIONS / CHANGELOG / DEPENDENCIES bijgewerkt
- prompts/2026-06-05_convert_v070_knuth.md → done

## Tests

- pytest 11/11 (9 oud + 2 docx + 2 xlsx + 3 ocr) — alle groen
- node --check OK

## HC55 deploy (zelfde sessie)

- `apt install tesseract-ocr tesseract-ocr-nld tesseract-ocr-eng ghostscript unpaper qpdf` (~250MB)
- venv pip install ocrmypdf
- git pull + rsync frontend + systemctl restart
- e2e smoke met gescande PDF

## Codename

**Brian Reid** — Scribe markup (1980), document-typografie-pionier. Past bij OCR ("tekst-herkenning"). Brotz blijft v1.0.0 reserve.

## Open

- Mail backend: Hostinger mailbox `pdfservice@icthorse.nl` (gebruikersactie)
- icthorse.nl reverse-proxy (Hostinger)
- P3 nginx-cleanup `.bak` (uitgesteld, niet-blokkerend)
- docs/screens/ (gebruikersactie browser)
- SRI-hashes
